import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    bodyCounter : {
        justifyContent: 'center', 
        alignItems: 'center',
        backgroundColor: '#aacc11'
    }
})

export default styles;